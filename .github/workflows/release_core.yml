# Copyright 2026 The HRX Authors
#
# Licensed under the Apache License v2.0 with LLVM Exceptions.
# See https://llvm.org/LICENSE.txt for license information.
# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

name: Release CMake Core

on:
  push:
    tags:
      - "v*.*.*"
      - "[0-9]*.[0-9]*.[0-9]*"

permissions:
  actions: read
  contents: read

jobs:
  validate_tag:
    name: Validate tag
    runs-on: ubuntu-24.04
    steps:
      - name: Check semantic version tag
        run: |
          tag="${GITHUB_REF_NAME}"
          if [[ ! "${tag}" =~ ^v?([0-9]|[1-9][0-9]*)\.([0-9]|[1-9][0-9]*)\.([0-9]|[1-9][0-9]*)(-[0-9A-Za-z-]+(\.[0-9A-Za-z-]+)*)?(\+[0-9A-Za-z-]+(\.[0-9A-Za-z-]+)*)?$ ]]; then
            echo "Not a semantic version tag: ${tag}" >&2
            exit 1
          fi

  build_core_linux:
    name: CMake Linux Release
    needs: validate_tag
    uses: ./.github/workflows/build_core_linux.yml
    permissions:
      contents: read
    with:
      name: Release
      release_type: nightly
      artifact_set: core
      build_type: RelWithDebInfo
      sanitizer: none
      test_gpu: false
      ctest_label_exclude_regex: "runtime-resource=|manual"
      # amdxdna HAL must be ON so the released runtime can drive the NPU;
      # without it hrx loads but silently no-ops on amdxdna executables.
      cmake_options: |
        -DIREE_HAL_DRIVER_AMDXDNA=ON
        -DIREE_HAL_AMDGPU_TARGETS=gfx942;gfx1151;gfx1201
      package: true
      package_suffix: ${{ github.ref_name }}

  build_core_windows:
    name: CMake Windows Release
    needs: validate_tag
    uses: ./.github/workflows/build_core_windows.yml
    permissions:
      contents: read
    with:
      name: Release
      windows_toolchain: msvc
      release_type: nightly
      artifact_set: core
      build_type: RelWithDebInfo
      # amdxdna HAL must be ON so hrx.dll can drive the NPU on Windows.
      cmake_options: |
        -DIREE_HAL_DRIVER_AMDXDNA=ON
      package: true
      package_suffix: ${{ github.ref_name }}

  publish_release:
    name: Publish release
    needs: [build_core_linux, build_core_windows]
    runs-on: ubuntu-24.04
    permissions:
      actions: read
      contents: write
    steps:
      - name: Download public package artifact (linux)
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: hrx-public-linux-x86_64
          path: release-assets

      - name: Download public dependency package artifact (linux)
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: hrx-public-deps-linux-x86_64
          path: release-assets

      - name: Download test package artifact (linux)
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: hrx-tests-linux-x86_64
          path: release-assets

      - name: Download ROCm build environment package artifact (linux)
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: hrx-rocm-buildenv-linux-x86_64
          path: release-assets

      - name: Download public package artifact (windows)
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: hrx-public-windows-msvc-x86_64
          path: release-assets

      - name: Download public dependency package artifact (windows)
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: hrx-public-deps-windows-msvc-x86_64
          path: release-assets

      - name: Download test package artifact (windows)
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: hrx-tests-windows-msvc-x86_64
          path: release-assets

      - name: Download ROCm build environment package artifact (windows)
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: hrx-rocm-buildenv-windows-msvc-x86_64
          path: release-assets

      - name: Validate release assets
        run: |
          tag="${GITHUB_REF_NAME}"
          expected_assets=(
            "hrx-public-linux-x86_64-${tag}.tar.zst"
            "hrx-public-linux-x86_64-${tag}.manifest.json"
            "hrx-public-linux-x86_64-env.sh"
            "hrx-public-deps-linux-x86_64-${tag}.tar.zst"
            "hrx-public-deps-linux-x86_64-${tag}.manifest.json"
            "hrx-public-deps-linux-x86_64-env.sh"
            "hrx-tests-linux-x86_64-${tag}.tar.zst"
            "hrx-tests-linux-x86_64-${tag}.manifest.json"
            "hrx-rocm-buildenv-linux-x86_64-${tag}.tar.zst"
            "hrx-rocm-buildenv-linux-x86_64-${tag}.manifest.json"
            "hrx-rocm-buildenv-linux-x86_64-env.sh"
            "hrx-public-windows-x86_64-${tag}.zip"
            "hrx-public-windows-x86_64-${tag}.manifest.json"
            "hrx-public-windows-x86_64-env.cmd"
            "hrx-public-windows-x86_64-env.ps1"
            "hrx-public-deps-windows-x86_64-${tag}.zip"
            "hrx-public-deps-windows-x86_64-${tag}.manifest.json"
            "hrx-public-deps-windows-x86_64-env.cmd"
            "hrx-public-deps-windows-x86_64-env.ps1"
            "hrx-tests-windows-x86_64-${tag}.zip"
            "hrx-tests-windows-x86_64-${tag}.manifest.json"
            "hrx-rocm-buildenv-windows-x86_64-${tag}.zip"
            "hrx-rocm-buildenv-windows-x86_64-${tag}.manifest.json"
            "hrx-rocm-buildenv-windows-x86_64-env.cmd"
            "hrx-rocm-buildenv-windows-x86_64-env.ps1"
          )

          missing=0
          for asset in "${expected_assets[@]}"; do
            if [[ ! -f "release-assets/${asset}" ]]; then
              echo "Missing release asset: ${asset}" >&2
              missing=1
            fi
          done
          if [[ "${missing}" != 0 ]]; then
            echo "Downloaded assets:" >&2
            find release-assets -maxdepth 2 -type f -print >&2 || true
            exit 1
          fi

          ls -lh release-assets

      - name: Compute SHA256 checksums
        run: |
          cd release-assets
          sha256sum \
            hrx-public-linux-x86_64-*.tar.zst \
            hrx-public-windows-x86_64-*.zip \
            > SHA256SUMS.txt
          cat SHA256SUMS.txt

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="${GITHUB_REF_NAME}"
          if [[ ! "${tag}" =~ ^v?([0-9]|[1-9][0-9]*)\.([0-9]|[1-9][0-9]*)\.([0-9]|[1-9][0-9]*)(-[0-9A-Za-z-]+(\.[0-9A-Za-z-]+)*)?(\+[0-9A-Za-z-]+(\.[0-9A-Za-z-]+)*)?$ ]]; then
            echo "Not a semantic version tag: ${tag}" >&2
            exit 1
          fi

          prerelease_args=()
          core_version="${tag#v}"
          core_version="${core_version%%+*}"
          if [[ "${core_version}" == *-* ]]; then
            prerelease_args+=(--prerelease)
          fi

          if gh release view "${tag}" --repo "${GITHUB_REPOSITORY}" >/dev/null 2>&1; then
            gh release upload "${tag}" release-assets/* \
              --repo "${GITHUB_REPOSITORY}" \
              --clobber
          else
            gh release create "${tag}" release-assets/* \
              --repo "${GITHUB_REPOSITORY}" \
              --title "HRX ${tag}" \
              --generate-notes \
              "${prerelease_args[@]}"
          fi
